The previous JSON object was rejected during validation. Return one complete corrected JSON object with `version` set to `1` and the full `updates` array. Include every valid update that should remain. The application replaces the previous attempt as one unit. Do not add Markdown or prose. If you cannot produce valid UI, return `{"version":1,"updates":[{"type":"message","text":"..."}]}`.
