OPENAI_API_KEY=""

# A single API key used for both modes:
# - When LLM_BASE_URL is set, LLM_API_KEY authenticates against that OpenAI-compatible
#   endpoint (e.g. https://api.deepseek.com).
# - When LLM_BASE_URL is unset, LLM_API_KEY authenticates against OpenRouter.
LLM_BASE_URL=""
LLM_API_KEY=""

LANGSMITH_TRACING="false"
LANGSMITH_API_KEY=""
LANGSMITH_PROJECT="deep-agent-template"
# Set this for non-US LangSmith regions, for example:
# LANGSMITH_ENDPOINT="https://eu.api.smith.langchain.com"
# Required only when an API key can access multiple workspaces:
# LANGSMITH_WORKSPACE_ID=""
# Use "true" for persistent processes and "false" for serverless runtimes:
LANGCHAIN_CALLBACKS_BACKGROUND="true"
