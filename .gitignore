node_modules
dist
.next
coverage
.turbo
.cache
packages/web-app/.data/

.DS_Store
*.log

.env
.env.*
!/.env.example

*.tsbuildinfo

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Sandbox workspace — bind-mounted into the compose-managed python-sandbox
# container. Filled with per-execution mkdtemp dirs at runtime.
sandbox-workspace/
